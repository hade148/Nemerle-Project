// Main.jack
// Entry point for the NEMERLE Objects Interactive Program.
// N = Notebook, E = Eyes (first and last letters of NEMERLE)

class Main {
    function void main() {
        var Game game;
        let game = Game.new();
        do game.run();
        do game.dispose();
        return;
    }
}
